apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: velero
  namespace: default
spec:
  interval: 5m
  chart:
    spec:
      releaseName: velero
      chart: velero
      version: '>=2.29.2'
      sourceRef:
        kind: HelmRepository
        name: vmware-tanzu
        namespace: flux-system
      interval: 1m